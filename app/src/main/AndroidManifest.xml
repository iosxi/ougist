<?xml version="1.0" encoding="utf-8"?>
<!-- package (namespace) は Gradle の namespace / tools のビルドスクリプトが与える -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- アイコンに指が当たったときの触覚フィードバックに使う。要求する権限はこれだけ。 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ランチャーに出るアプリだけを列挙する。QUERY_ALL_PACKAGES は使わない。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/OugistTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SlotListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:parentActivityName=".SlotListActivity" />

        <service
            android:name=".OugistService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
